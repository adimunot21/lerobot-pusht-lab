# Python dependencies for lerobot-pusht-lab.
#
# Strategy: pin only the packages whose version matters for correctness.
# Everything else is left to pip's resolver — that way the same file works
# across Python versions (3.10, 3.11, 3.12) and across host environments
# (Ubuntu / RunPod PyTorch template / Colab / etc.).
#
# Critical pins:
#   - lerobot: pinned to specific commit (fast-moving repo, API churns)
#   - pymunk<7: gym-pusht 0.1.6 calls Space.add_collision_handler() which
#     was removed in pymunk 7.x. Discovered Phase 2 smoke test 2026-05-10.

# Core: LeRobot pinned to commit + the env
lerobot @ git+https://github.com/huggingface/lerobot.git@f90db58c15a998a8ae37780ccd255bbde0130e00
gym-pusht
pymunk<7

# Logging / experiment tracking (offline mode used)
wandb

# Configuration / persistence
PyYAML
python-dotenv
safetensors

# Phase 5 plotting + report generation
matplotlib

# Dev tooling
pytest
ruff

# torch + torchvision intentionally not pinned here — RunPod's PyTorch image
# ships with them pre-installed at versions matching the host CUDA. Letting
# them be reinstalled would pull non-CUDA wheels and break GPU access.
